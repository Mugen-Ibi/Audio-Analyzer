# 全面レビュー・改修計画（2026-09-09）

対象: 全Rustソース、依存設定、CI、README、開発ガイド。開始時のUI編集は保持する。
既存17テストとClippyは成功。ただし以下の境界条件は未検証だった。

## 実施順

1. P1: Reference無効時のルート復号アンダーフローを修正し、モノラル経路を検証する。
2. P1: ルーティング変更前の解析結果・保持値が新しい入力の結果として表示されないよう世代管理する。
3. P2: DSPのFFT作業領域を再利用し、非有限入力による平均値の永続汚染を防ぐ。
4. P2: 結果キュー満杯時の最新結果消失とワーカー終了応答を改善する。
5. P2: 履歴帯域のピーク欠落、ピークホールド時間、履歴初期化・無入力表示・メーター境界を修正する。
6. 回帰テスト、fmt、Clippy、debug/releaseテスト、ビルドを実施し、ドキュメントと検証結果を更新する。

## 完了基準

レビューで確認した現行機能の不具合を修正し、再現テストと静的検査を通す。
READMEの将来機能（信号生成、別デバイス同期等）は新規機能のロードマップとして区別する。
実機・ドライバー・目視に依存する項目は実施の有無を明記し、未検証を解決済みとはしない。

## 改修結果

| 優先度 | 指摘 | 対応 |
| --- | --- | --- |
| P1 | `then_some`の引数が先に評価され、Reference無効で減算がアンダーフロー | `checked_sub`で復号、無効ルートの往復テスト |
| P1 | 入力切替後に旧入力のキュー・保持値が残る | Snapshotに世代を追加して受信時に除外、画面の履歴・メーター・HOLDをリセット |
| P1 | 開始成功を返してもFFT窓分のデータを出さないドライバー | 2秒の入力到着確認とバックエンド切替、全失敗時は原因を返す |
| P2 | FFTごとの一時メモリ確保 | `process_with_scratch`と事前確保した作業領域へ変更 |
| P2 | NaN/Infinity入力でFFT・平均値が汚染される | 非有限サンプルをゼロへ置換。通常の有限入力の振幅は保持 |
| P2 | キュー満杯で最後の解析結果が永久に消失 | 未送信の最新1件を保持し、入力停止後も再送 |
| P2 | 入力継続中にワーカーの終了要求を確認しない | キュー消費ループ内でも確認 |
| P2 | 入力切替時の未完成ブロックの欠落が未計上 | 欠落カウンターに加算 |
| P2 | 履歴が対数帯域の代表ビンしか読まず、高域ピークを失う | 全可聴域FFTビンを対数帯域へ割り当て最大値を記録 |
| P2 | スコープの固定間引きで周期16サンプルの信号が無音に見える | 区間ごとの最大振幅ペアを選択（密度の統計表示ではない） |
| P2 | PeakHoldが保持期間の一部も減衰時間に算入 | 保持期限以降の経過時間だけ減衰 |
| P2 | 起動時に0 dBFS、入力停止時も正常と表示 | 初期値を-120 dBFSにし、入力結果の鮮度を表示 |
| P2 | 無効履歴の時刻、Reference欠落区間の誤接続、空メーター点灯 | ゼロレート拒否、時刻巻戻しリセット、線分切断、メーター境界修正 |
| P3 | ガイドのビットパーフェクト表現が実装と不一致 | 同一ストリーム同期・WASAPI共有モードの制約に統一 |

## 検証結果

- Rust 1.95.0、Windows環境。
- `cargo test --locked`: 30件成功。実機テスト1件は明示的な実行を必要とするため通常はignore。
- `cargo test --release --locked`: 30件成功。
- `cargo clippy --locked --all-targets --all-features -- -D warnings`: 成功。
- `cargo fmt --check`、`git diff --check`: 成功。
- 48/96/192kHzは合成データで検証。全可聴FFTビンのピーク保存、世代切替、キュー再送・終了を含む。
- 760/1440pxでウィジェットのヘッドレス描画に成功。実ウィンドウの目視検証とは区別する。
- CIにもreleaseテストを追加。リモートCIの実行結果は未確認。

## 残存する実機ブロッカー（2026-09-09時点）

`cargo test --locked --test live_audio -- --ignored --nocapture` を実行したが失敗した。

1. Realtek ASIO（48kHz、2ch）は開始を受理するが、観測した入力は1,584フレームで止まり、最初のFFT窓が完成しない。開始時検証でこの経路を除外するよう修正済みだが、ドライバー停止自体の原因は未確定。
2. 続く既定WASAPI入力は `Failed to initialize audio client` / `0x80070057`（パラメーターが間違っています）で失敗。固定バッファ指定とデバイス既定バッファ指定の両方で再現。
3. このため実入力でのルート変更・連続測定・性能・96/192kHz動作、ネイティブUI目視確認は未完了。正常に動く入力デバイスと接続状態の確認が必要。

ハードウェアが利用できる状態になったら同じ実機テストを再実行し、ASIO/WASAPIの初期化・連続受信を切り分ける。テスト失敗を成功扱いにする変更は行っていない。READMEの将来機能は今回実装していない。

## 2026-09-10 再設計後の追記

要件・比較案・実装・最新の検証結果は [ARCHITECTURE.md](ARCHITECTURE.md) に移した。
通常テストは42件成功し、ASIO SDKを補完した環境でdebug/release、Clippy、releaseビルドが成功。
実入力テストもMOTU M Series（ASIO、96kHz、6ch）で成功した。制御スレッドからの接続、Reference無効化・復元、停止・再接続・終了を確認した。
上記のRealtek ASIOとWASAPIの問題の再検証、実機48/192kHz、長時間性能測定、ネイティブUI目視は未実施。
